services:
  mongodb:
    image: mongo:7.0
    container_name: librarymanager-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: librarymanager
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data: